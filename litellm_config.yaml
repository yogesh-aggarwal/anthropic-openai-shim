include:
  - models.yaml

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY

litellm_settings:
  drop_params: true
  num_retries: 2
  retry_after: 1
  allowed_fails: 1
  cooldown_time: 60
  # Global proxy is now handled via environment variables in compose.yml
